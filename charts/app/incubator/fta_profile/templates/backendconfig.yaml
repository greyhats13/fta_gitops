{{- if .Values.ingress.backendConfig }}
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ include "fta-dev-svc-gce.fullname" . }}-backendconfig
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fta-dev-svc-gce.labels" . | nindent 4 }}
spec:
  {{- toYaml .Values.ingress.backendConfig | nindent 2 }}
{{- end }}