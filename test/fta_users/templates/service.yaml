apiVersion: v1
kind: Service
metadata:
  name: {{ include "fta-uses.fullname" . }}
  labels:
    {{- include "fta-uses.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fta-uses.selectorLabels" . | nindent 4 }}
