apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fta-uses.fullname" . }}-test-connection"
  labels:
    {{- include "fta-uses.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fta-uses.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
